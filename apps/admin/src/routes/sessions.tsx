export function SessionsPage() {
  return (
    <div>
      <h1 className="text-xl font-bold mb-6" style={{ color: "var(--text)" }}>Sessões</h1>
      <div
        className="rounded-xl p-8 text-center text-sm"
        style={{ background: "var(--surface)", border: "1px dashed var(--dim)", color: "var(--muted)" }}
      >
        Gestão de sessões em breve
      </div>
    </div>
  );
}
