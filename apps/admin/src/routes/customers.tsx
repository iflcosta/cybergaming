export function CustomersPage() {
  return (
    <div>
      <h1 className="text-xl font-bold mb-6" style={{ color: "var(--text)" }}>Clientes</h1>
      <div
        className="rounded-xl p-8 text-center text-sm"
        style={{ background: "var(--surface)", border: "1px dashed var(--dim)", color: "var(--muted)" }}
      >
        Lista de clientes em breve
      </div>
    </div>
  );
}
